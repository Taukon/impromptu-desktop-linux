name: CI

on:
  push:
  pull_request:
    paths:
      - 'src/**'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3

      - name: Run lint
        run: |
          npm install --only=dev
          npm run lint:ci
